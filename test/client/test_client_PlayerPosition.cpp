//
// Created by louis on 11/12/23.
//
#include <boost/test/unit_test.hpp>
#include "../../src/client/render.h"

/*
namespace render {
    BOOST_AUTO_TEST_CASE(firstPlayerPos) {
        float X = 0;
        float Y = 0;
        int playerNum = 0;
        PlayerPosition* PlayerPositionTest = new PlayerPosition(X, Y, playerNum);
        float Y2 = PlayerPositionTest->get_pixelY();
        float X2 = PlayerPositionTest->get_pixelX();
        int playerNum2 = PlayerPositionTest->get_playerNum();
        PlayerPositionTest->set_pixelY(Y);
        PlayerPositionTest->set_pixelX(X);
        PlayerPositionTest->set_playerNum(playerNum);
        BOOST_CHECK_EQUAL(X, X2);
        BOOST_CHECK_EQUAL(Y, Y2);
        BOOST_CHECK_EQUAL(playerNum, playerNum2);
    }
}*/
